apiVersion: v1
kind: Service
metadata:
  name: kube-news-postgres
  labels:
    app.kubernetes.io/name: kube-news
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kube-news
    app.kubernetes.io/component: database
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
